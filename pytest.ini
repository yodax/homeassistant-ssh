[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -q --strict-markers
# Home Assistant reports deprecations through the logger rather than
# warnings.warn, so the guard lives in the fail_on_ha_deprecation fixture in
# tests/conftest.py, not here.
